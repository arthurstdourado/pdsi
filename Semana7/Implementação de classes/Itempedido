/**
 * Representa um item dentro de um pedido: um produto e a quantidade solicitada.
 */
public class ItemPedido {

    private Produto produto;
    private int quantidade;

    public ItemPedido(Produto produto, int quantidade) {
        if (quantidade <= 0) {
            throw new IllegalArgumentException("Quantidade deve ser maior que zero.");
        }
        this.produto = produto;
        this.quantidade = quantidade;
    }

    public Produto getProduto() {
        return produto;
    }

    public int getQuantidade() {
        return quantidade;
    }

    /** Subtotal = preço unitário do produto x quantidade solicitada. */
    public double calcularSubtotal() {
        return produto.getPrecoUnitario() * quantidade;
    }

    @Override
    public String toString() {
        return String.format("%dx %s = R$ %.2f", quantidade, produto.getNome(), calcularSubtotal());
    }
}
